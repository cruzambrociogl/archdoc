# A dotenv file the compose file names through env_file. Unlike a .env.example, this one is
# what the stack actually loads, so its values are facts about the system.
QUEUE_URL=amqp://broker.example.com:5672
SEARCH_HOST=search
SEARCH_PORT=9200
QUEUE_PASSWORD=hunter2
